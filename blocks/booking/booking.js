export default function decorate(block) {  function getFormattedDate(offsetDays = 0) {    const date = new Date();    date.setDate(date.getDate() + offsetDays);    return date.toISOString().split('T')[0];  }  function getDisplayDate(offsetDays = 0) {    const date = new Date();    date.setDate(date.getDate() + offsetDays);    return date.toLocaleDateString('en-GB', {      day: '2-digit',      month: 'short',      year: '2-digit',    }).replace(/ /g, ' ');  }  block.textContent = '';  const bookingForm = document.createElement('div');  bookingForm.className = 'booking-form';  const form = document.createElement('form');  form.className = 'booking-form-container';  const fields = [    {      type: 'text',      name: 'destination',      placeholder: 'Where to?',      label: 'Destination',      required: true,    },    {      type: 'date',      name: 'checkin',      label: 'Check-in Date',      required: true,    },    {      type: 'date',      name: 'checkout',      label: 'Check-out Date',      required: true,         },    {      type: 'select',      name: 'guests',      label: 'No. of Guests & Rooms',      options: [        { value: '1', text: '1 Adult, 1 Room' },        { value: '2', text: '2 Adults, 1 Room' },        { value: '3', text: '2 Adults, 2 Rooms' },        { value: '4', text: '3 Adults, 2 Rooms' },        { value: '5', text: '4 Adults, 2 Rooms' },      ],      required: true,    },  ];  fields.forEach((field) => {    const fieldContainer = document.createElement('div');    fieldContainer.className = 'booking-field';    const label = document.createElement('label');    label.textContent = field.label;    label.setAttribute('for', field.name);    let input;    if (field.type === 'select') {      input = document.createElement('select');      input.name = field.name;      input.id = field.name;      input.required = field.required;      const defaultOption = document.createElement('option');      defaultOption.value = '';      defaultOption.textContent = '1 Adult, 1 Room';      defaultOption.selected = true;      input.appendChild(defaultOption);      field.options.forEach((option) => {        const optionElement = document.createElement('option');        optionElement.value = option.value;        optionElement.textContent = option.text;        input.appendChild(optionElement);      });    } else {      input = document.createElement('input');      input.type = field.type;      input.name = field.name;      input.id = field.name;      input.placeholder = field.placeholder || '';      input.required = field.required;      if (field.name === 'checkin') {        const today = new Date().toISOString().split('T')[0];        input.min = today;        input.addEventListener('change', () => {          const checkoutField = form.querySelector('#checkout');          if (checkoutField) {            checkoutField.min = input.value;          }        });      }      if (field.name === 'checkout') {        const checkinField = form.querySelector('#checkin');        if (checkinField && checkinField.value) {          input.min = checkinField.value;        }      }    }    fieldContainer.appendChild(label);    fieldContainer.appendChild(input);    form.appendChild(fieldContainer);  });  const searchButton = document.createElement('button');  searchButton.type = 'submit';  searchButton.className = 'booking-search-btn';  searchButton.textContent = 'Search';  form.appendChild(searchButton);  form.addEventListener('submit', (e) => {    e.preventDefault();    const formData = new FormData(form);    const bookingData = {      destination: formData.get('destination'),      checkin: formData.get('checkin'),      checkout: formData.get('checkout'),      guests: formData.get('guests'),    };    console.log('Searching for hotels with:', bookingData);    const successMessage = document.createElement('div');    successMessage.className = 'booking-success';    successMessage.textContent = `Searching for hotels in ${bookingData.destination} for ${bookingData.guests} guests from ${bookingData.checkin} to ${bookingData.checkout}`;    const existingMessage = bookingForm.querySelector('.booking-success');    if (existingMessage) {      existingMessage.remove();    }    bookingForm.appendChild(successMessage);    setTimeout(() => {      if (successMessage.parentNode) {        successMessage.remove();      }    }, 5000);  });  bookingForm.appendChild(form);  block.appendChild(bookingForm);}